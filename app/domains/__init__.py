# -*- coding: utf-8 -*-
"""业务域层：一个模块 = 一个限界上下文（机加 DFM 的一张主表或一组紧密相关的表）。

每个模块只声明**自己这张表长什么样**（字段登记表 / 附件槽位 / 外键 / 逻辑删除与排序策略）
以及**自己这份数据的读写与读模型还原**。模块之间**不互相 import**：

* 需要公共行表能力 → 继承 ``app.db.rows.ProjectRows``；
* 需要指向别的域的表名 → 读 ``app.db.tables`` 的登记表；
* 需要跨域编排（一次保存同时改工序与刀具行、写变更流水）→ 交给 ``app.services.store``。

这样依赖方向永远是单向的（``domains → db → core``），加一个新业务域不需要动任何既有域。
"""
