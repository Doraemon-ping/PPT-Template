# 编辑台 Product Design 验收（2026-09-03）

final result: passed

## 视觉依据与比较条件

- source visual truth: `output/editor-ux-qa/before.png`（改动前的现有工作台）。
- implementation screenshot: `output/editor-ux-qa/after.png`（最终工作台）；新增状态：`reuse.png`、`formula.png`，均在同目录。
- URL：`http://127.0.0.1:8000/template-editor`。
- CSS viewport：1280 × 720，浏览器报告 DPR 1.75；截图服务输出两图均 1265 × 712 像素，采用相同截图服务密度，无单边拉伸。
- state：模板 2、示例数据、原报告 1 页 / 9 处绑定、75% 画布缩放、实时预览。方案选择框显示 test 不等于载入它；当前原报告不是 test 的 10 处绑定副本。
- 全图比较：源图和最终图在同一个工具输出中并列提供给视觉检查。源图滚动约 37px，最终图滚动 0；按卡片和 PPT 内容区域对齐判断，不将这一纵向位移当成设计偏差。
- 特写：两张全图的 PPT、文字、按钮已可辨读，无需二次放大裁剪；另实际打开 `formula.png` 检查公式面板字体、边距、操作按钮，打开 `reuse.png` 检查选择器与真实页预览。公式图包含临时测试副本，随后已删除，不是用户最终数据。

## Findings

无剩余 P0/P1/P2 桌面界面问题。保留原设计，不新增独立设计系统。

| 必查表面 | 结果 |
| --- | --- |
| 字体与层次 | 沿用原中文无衬线及正文层次；新增公式输入 14px/1.6；中文参数不会裸露成代码路径。旧 `ole` 标题改为「公式」。 |
| 间距与布局 | 保持左侧方案/页面，右侧 PPT。新增对象行和复用入口带来的下方内容位移属于需求变化。弹窗选择区与预览区不重叠；公式长内容可滚动，保存按钮可达。 |
| 颜色与状态 | 保持橙色操作按钮、浅灰背景、绿色已绑定；新增焦点轮廓，提示文字加深。未保存/缺数据/错误均有文字信息。 |
| 图片保真 | 继续使用 PowerPoint 生成的原页 PNG；同缩放下表格、批注、公式与背景保持原外观。未以 HTML/CSS 图形重画 PPT。 |
| 文案 | 「从其他方案插入页面」明确对象是保存方案；来源页码、绑定数量、独立副本、当前数据预览均有提示；明确公式只编排显示，不执行计算代码。 |

## 比较历史

1. 初轮检查：复用对话框布局通过；公式和草稿流程检查发现需要显式撤销入口，已在验收前补上并通过操作测试。
2. 截图对齐阶段：不同滚动位置及鼠标悬停会呈现已有橙色命中区域。没有将这些状态差异误记为 UI 回归；最终刷新后截取无悬停版本，与源图共同检查。
3. 最终对照：新增入口、对象行属有意变更；无需进一步视觉修复。没有剩余阻塞项。

## 已验证交互

- 复用已保存方案 test 第 1 页：带入 10 处绑定，原报告第 1 页 9 处不变。
- 多页来源 TP-DFM模板-v1.0：选择方案第 2 页（official 原第 2 页），实时预览成功；取消不插入。
- 公式对象搜索和选择，旧公式中文参数往返、分式显示、参数插入、实时 PNG、空白分母校验。
- 修改测试副本公式，保存后刷新仍保留；跨对象草稿恢复；撤销回到已保存状态。
- 测试副本上移/下移，并只移除测试副本；当前报告恢复原 1 页。四份服务器方案文件 SHA256 均未改变。
- 截至 2026-09-03 的页面验收快照为 Python 172 项、Node 6 项通过；当前全量回归为 Python 223 项、Node 12 项，见 [项目更新记录](docs/CHANGELOG.md)。初始页面载入检查未发现控制台错误；本轮后续交互以 DOM/截图/状态提示验收，未完成全程 console 监听。

## Open Questions / 测试缺口

- 本次桌面流程验收通过；移动/平板仅有响应式实现，未进行实机全流程截图验收。
- 保存草稿仅在本次网页会话中保留。浏览器刷新前仍应保存绑定。
- 原 84 页正式模板跨文件导出的兼容性问题仍以既有交接说明为准，本次未声称解决。

## Implementation Checklist

- [x] 复用来源是已保存方案中的单页，独立深拷贝，不改来源。
- [x] 中文公式编辑器与当前数据预览、错误提示。
- [x] 对象导航、状态、撤销、重复页实例草稿隔离。
- [x] 保留真实 PPT 预览及现有导入不同 PPT 的入口。
- [x] 清理测试副本、回归测试、更新 README 与增量交接。

## Follow-up Polish

- 后续可在用户实际屏幕尺寸下专项验收移动端/平板，并按反馈决定是否将长公式编辑改为侧栏。
